import { MagnifyDirective } from './magnify.directive';

describe('MagnifyDirective', () => {
  it('should create an instance', () => {
    const directive = new MagnifyDirective();
    expect(directive).toBeTruthy();
  });
});
